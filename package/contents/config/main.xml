<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
                          http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
  <kcfgfile name=""/>

  <group name="General">
    <!-- Comma-separated list of systemd units to watch; empty = all -->
    <entry name="watchedUnits" type="String">
      <default></default>
    </entry>

    <!-- Maximum log lines to keep in memory -->
    <entry name="maxRows" type="Int">
      <default>5000</default>
    </entry>

    <!-- Auto-start log watching when widget loads -->
    <entry name="autoStart" type="Bool">
      <default>true</default>
    </entry>

    <!-- Minimum priority to alert on (0=emerg ... 3=error) -->
    <entry name="alertPriority" type="Int">
      <default>3</default>
    </entry>

    <!-- Enable system (KNotification) alerts -->
    <entry name="notificationsEnabled" type="Bool">
      <default>true</default>
    </entry>

    <!-- Minimum priority that triggers a system notification (0=emerg…7=debug) -->
    <!-- 2=critical, 3=error, 4=warning, 5=notice -->
    <entry name="notificationMinPriority" type="Int">
      <default>3</default>
    </entry>

    <!-- Info Bar ──────────────────────────────────────────────────────────── -->
    <!-- Master switch -->
    <entry name="infoBarEnabled" type="Bool"><default>true</default></entry>

    <!-- Visibility toggles -->
    <entry name="infoShowHostname"     type="Bool"><default>true</default></entry>
    <entry name="infoShowKernel"       type="Bool"><default>true</default></entry>
    <entry name="infoShowPlasma"       type="Bool"><default>true</default></entry>
    <entry name="infoShowKF"           type="Bool"><default>true</default></entry>
    <entry name="infoShowQt"           type="Bool"><default>true</default></entry>
    <entry name="infoShowWindowSystem" type="Bool"><default>true</default></entry>
    <entry name="infoShowUptime"       type="Bool"><default>true</default></entry>

    <!-- Custom short labels (shown in the bar; tooltip shows the full name) -->
    <entry name="infoLabelHostname"     type="String"><default>Host</default></entry>
    <entry name="infoLabelKernel"       type="String"><default>Kernel</default></entry>
    <entry name="infoLabelPlasma"       type="String"><default>Plasma</default></entry>
    <entry name="infoLabelKF"           type="String"><default>KF</default></entry>
    <entry name="infoLabelQt"           type="String"><default>Qt</default></entry>
    <entry name="infoLabelWindowSystem" type="String"><default>WS</default></entry>
    <entry name="infoLabelUptime"       type="String"><default>Uptime</default></entry>

    <!-- Global appearance -->
    <entry name="infoShowIcons"   type="Bool"><default>false</default></entry>
    <entry name="infoValueInline" type="Bool"><default>true</default></entry>
  </group>
</kcfg>
